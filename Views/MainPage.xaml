<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodel="clr-namespace:AuroraForecast.ViewModels"
             xmlns:model="clr-namespace:AuroraForecast.Models"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="AuroraForecast.Views.MainPage"
             Title="{Binding Title}">

    <ContentPage.BindingContext>
        <viewmodel:MainPageViewModel />
    </ContentPage.BindingContext>

    <Grid>
        <!-- Video Background -->
        <!--<toolkit:MediaElement
            x:Name="BackgroundVideo"
            Source="{Binding CurrentVideoSource}"
            ShouldAutoPlay="True"
            ShouldLoopPlayback="True"
            ShouldShowPlaybackControls="False"
            Aspect="AspectFill"
            Volume="0"
            VerticalOptions="FillAndExpand"
            HorizontalOptions="FillAndExpand" />-->

        <!-- Semi-transparent overlay -->
        <BoxView Color="Black" Opacity="0.4" />

        <!-- Main Content -->
        <ScrollView>
            <VerticalStackLayout Padding="20" Spacing="20">
                
                <!-- Header -->
                <Label 
                    Text="🌌 Aurora Forecast"
                    FontSize="32"
                    FontAttributes="Bold"
                    TextColor="White"
                    HorizontalOptions="Center"
                    Margin="0,20,0,0" />

                <!-- Search Section -->
                <Frame BackgroundColor="#40FFFFFF" 
                       CornerRadius="15"
                       Padding="15"
                       HasShadow="True">
                    <VerticalStackLayout Spacing="10">
                        <Label 
                            Text="Sök efter stad"
                            FontSize="16"
                            TextColor="White"
                            FontAttributes="Bold" />
                        
                        <HorizontalStackLayout Spacing="10">
                            <Entry 
                                x:Name="CityEntry"
                                Text="{Binding CityName}"
                                Placeholder="T.ex. Uppsala, Kiruna..."
                                PlaceholderColor="#CCFFFFFF"
                                TextColor="White"
                                BackgroundColor="#60000000"
                                HorizontalOptions="FillAndExpand"
                                Completed="OnSearchClicked" />
                            
                            <Button 
                                Text="🔍"
                                Command="{Binding SearchCityCommand}"
                                BackgroundColor="#8A2BE2"
                                TextColor="White"
                                WidthRequest="60"
                                HeightRequest="50"
                                CornerRadius="10" />
                        </HorizontalStackLayout>
                    </VerticalStackLayout>
                </Frame>

                <!-- Error Message -->
                <Frame 
                    IsVisible="{Binding HasError}"
                    BackgroundColor="#80FF4444"
                    CornerRadius="10"
                    Padding="15">
                    <Label 
                        Text="{Binding ErrorMessage}"
                        TextColor="White"
                        FontSize="14" />
                </Frame>

                <!-- Loading Indicator -->
                <ActivityIndicator 
                    IsRunning="{Binding IsBusy}"
                    IsVisible="{Binding IsBusy}"
                    Color="White"
                    HeightRequest="50" />

                <!-- Current Forecast -->
                <Frame 
                    IsVisible="{Binding IsDataLoaded}"
                    BackgroundColor="#60FFFFFF"
                    CornerRadius="20"
                    Padding="20"
                    HasShadow="True">
                    <VerticalStackLayout Spacing="15">
                        
                        <Label 
                            Text="📍 Aktuell Prognos"
                            FontSize="20"
                            FontAttributes="Bold"
                            TextColor="White" />
                        
                        <Label 
                            Text="{Binding LocationInfo}"
                            FontSize="14"
                            TextColor="#E0E0E0" />

                        <BoxView Color="White" Opacity="0.3" HeightRequest="1" />

                        <!-- Kp Index Display -->
                        <Grid ColumnDefinitions="*,*" RowDefinitions="Auto,Auto" ColumnSpacing="10">
                            <VerticalStackLayout Grid.Column="0" Spacing="5">
                                <Label 
                                    Text="Kp-Index"
                                    FontSize="14"
                                    TextColor="#E0E0E0" />
                                <Label 
                                    Text="{Binding CurrentKpIndex, StringFormat='{0:F1}'}"
                                    FontSize="36"
                                    FontAttributes="Bold"
                                    TextColor="#FFD700" />
                            </VerticalStackLayout>

                            <VerticalStackLayout Grid.Column="1" Spacing="5">
                                <Label 
                                    Text="Sannolikhet"
                                    FontSize="14"
                                    TextColor="#E0E0E0" />
                                <Label 
                                    Text="{Binding Probability, StringFormat='{0}%'}"
                                    FontSize="36"
                                    FontAttributes="Bold"
                                    TextColor="#00FF7F" />
                            </VerticalStackLayout>
                        </Grid>

                        <Frame 
                            BackgroundColor="#40000000"
                            CornerRadius="10"
                            Padding="15"
                            Margin="0,10,0,0">
                            <VerticalStackLayout Spacing="8">
                                <Label 
                                    Text="{Binding ActivityLevel}"
                                    FontSize="18"
                                    FontAttributes="Bold"
                                    TextColor="White"
                                    HorizontalOptions="Center" />
                                <Label 
                                    Text="{Binding ActivityDescription}"
                                    FontSize="14"
                                    TextColor="#E0E0E0"
                                    HorizontalOptions="Center"
                                    HorizontalTextAlignment="Center" />
                            </VerticalStackLayout>
                        </Frame>
                    </VerticalStackLayout>
                </Frame>

                <!-- 3-Day Forecast -->
                <Frame 
                    IsVisible="{Binding IsDataLoaded}"
                    BackgroundColor="#60FFFFFF"
                    CornerRadius="20"
                    Padding="20"
                    HasShadow="True">
                    <VerticalStackLayout Spacing="15">
                        
                        <Label 
                            Text="📅 3-Dagars Prognos"
                            FontSize="20"
                            FontAttributes="Bold"
                            TextColor="White" />

                        <CollectionView ItemsSource="{Binding ForecastItems}">
                            <CollectionView.ItemTemplate>
                                <DataTemplate x:DataType="model:ForecastDay">
                                    <Frame 
                                        BackgroundColor="#40000000"
                                        CornerRadius="10"
                                        Padding="15"
                                        Margin="0,5">
                                        <Grid ColumnDefinitions="50,*,80" ColumnSpacing="10">
                                            <Label 
                                                Grid.Column="0"
                                                Text="{Binding IconEmoji}"
                                                FontSize="30"
                                                VerticalOptions="Center" />
                                            
                                            <VerticalStackLayout Grid.Column="1" VerticalOptions="Center">
                                                <Label 
                                                    Text="{Binding Date}"
                                                    FontSize="16"
                                                    FontAttributes="Bold"
                                                    TextColor="White" />
                                                <Label 
                                                    Text="{Binding ActivityLevel}"
                                                    FontSize="12"
                                                    TextColor="#E0E0E0" />
                                            </VerticalStackLayout>

                                            <Label 
                                                Grid.Column="2"
                                                Text="{Binding KpIndex, StringFormat='Kp {0:F1}'}"
                                                FontSize="16"
                                                FontAttributes="Bold"
                                                TextColor="#FFD700"
                                                VerticalOptions="Center"
                                                HorizontalOptions="End" />
                                        </Grid>
                                    </Frame>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </VerticalStackLayout>
                </Frame>

                <!-- Info Footer -->
                <Label 
                    Text="Data från NOAA Space Weather Prediction Center"
                    FontSize="10"
                    TextColor="#B0B0B0"
                    HorizontalOptions="Center"
                    Margin="0,10,0,20" />

            </VerticalStackLayout>
        </ScrollView>
    </Grid>

</ContentPage>
